<!--
GENERATED by autodoc-harness on 2026-07-14T17:14:40.700206+00:00 using model "ollama_chat/gemma4:e4b".
Do not hand-edit - regenerate via `autodoc-harness generate` instead, or your
changes will be silently overwritten on the next run.
-->

# API Reference

This document details the public interfaces available across the system components.

## Main Application Interface (`cli-interface`)

The `Main Application Interface` is a CLI tool responsible for managing the entire execution flow, including argument parsing, type validation, and orchestrating the core division logic. It handles multiple failure modes by returning specific exit codes (0 for success, 1 for input/math errors, 2 for usage errors).

### Functions

#### `main()`
**Signature:** `def main() -> int:`
**Description:** The primary entry point function that manages the CLI workflow. It processes command-line arguments (`sys.argv`), validates that exactly two arguments are provided, converts them to floats, calls the core division logic, and determines the final exit code based on success or failure (cites: `src/main.py:8-10`, `src/main.py:12-14`, `src/main.py:16-18`).

#### `divide(a: float, b: float)`
**Signature:** `def divide(a: float, b: float) -> float:`
**Description:** Performs the floating-point division of 'a' by 'b'. This function includes specialized logic to detect and warn about near-zero divisors (precision loss) and explicitly raises an error for zero division. It is called internally by `main()` (cites: `src/calculator.py:13-14`, `src/calculator.py:18-21`, `src/calculator.py:24-26`).

***

## Division Utility (`calculator-logic`)

This module provides the core business logic for dividing two floating-point numbers (`a / b`). It is designed to handle both explicit mathematical errors (zero division) and subtle numerical issues (precision loss).

### Functions

#### `divide(a: float, b: float)`
**Signature:** `def divide(a: float, b: float) -> float:`
**Description:** Divides the first argument (`a`) by the second argument (`b`). This function handles zero division by raising a descriptive `ZeroDivisionError` (cites: `src/calculator.py:10-23`). It also implements logic to issue a warning if the divisor is near zero, alerting the user to potential precision loss before returning the calculated value (cites: `src/calculator.py:17-20`, `src/calculator.py:21-23`).
